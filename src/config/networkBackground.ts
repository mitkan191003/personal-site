export const networkBackgroundOptions = {
  minNetworkSettings: { density: 14000, maxNodes: 100, maxDistance: 160 },
  maxNetworkSettings: { density: 1000, maxNodes: 2000, maxDistance: 80 },
  defaultSliderPosition: 0.05,
} as const;
